function preload(){
//insert filter img link
}
function setup(){
    canvas = createCanvas(550,550);
    canvas.center();//store canvas in var and place it in center
}

function draw(){
    //access webcam and apply filter
}
function take_snapshot(){
    save('mustache_filter_image.png');//save img
}